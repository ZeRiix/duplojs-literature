TypeScript deserves utilities built for what the language can be, not only for what JavaScript used to be.

That’s why we built @duplojs/utils:
immutable, composable, zero-dependency utilities that make typing an ally, not a constraint.

https://utils.duplojs.dev

#TypeScript #FP